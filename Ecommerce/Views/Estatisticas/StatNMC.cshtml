@model IEnumerable<Ecommerce.Models.Stat>

@{
    ViewBag.Title = "StatNMC";
}

<h2>Média de vendas por empresa</h2>

<p>
  
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.qtd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.qtd)
        </td>
        
    </tr>
}

</table>
<p>
    @Html.ActionLink("Voltar atrás", "Index")
</p>